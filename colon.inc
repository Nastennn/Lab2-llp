%define next_el 0
%macro colon 2 	; начало макроса - принимает 2 параметра: строку и указатель
%2:
dq next_el 		; временная переменная для хранения указателя на следующую строку
db %1, 0
%define next_el %2 ; константа для адреса следующей строки
%endmacro
